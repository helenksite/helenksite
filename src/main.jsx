import React from "react";
import ReactDOM from "react-dom/client";
import HelenKStore from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HelenKStore />
  </React.StrictMode>
);
